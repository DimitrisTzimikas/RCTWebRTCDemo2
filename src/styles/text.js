import { StyleSheet } from "react-native";


const text = StyleSheet.create({
  style: {
    fontSize: 20,
    textAlign: "center",
    margin: 10,
    borderRadius: 10,
  },
});

export default text;