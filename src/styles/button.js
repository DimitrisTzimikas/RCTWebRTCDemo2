import { StyleSheet } from "react-native";


const button = StyleSheet.create({
  container: {
    alignItems: 'center',
  },
  style: {
    textAlign: 'center',
    borderWidth: 1,
    borderColor: 'black',
    width: '50%',
    margin: 10,
    padding: 10,
    borderRadius: 10,
  },
});

export default button;